.env
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
data/
*.db
*.db-wal
*.db-shm
.cache/

# Sample output is deliberately committed so the submission can be evaluated without an
# API key -- see samples/README.md.
!samples/*.db

# Editor/tooling scratch, not part of the project.
.claude/
